// AUTO-GENERATED by .tooling/extract-units.py -- DO NOT EDIT BY HAND.
// Source workflow : wf2
// Node name       : Build Questions
// Node id         : 9b9840de-4764-4ac2-8c7d-c988608d03e8
// Node mode       : runOnceForAllItems
// sha256(jsCode)  : d91687ec77f9951265623bfadb239960e93e38989db78f6e562ff9cacbcb2d12
//
// The code between the BEGIN/END markers below is byte-identical to the
// node's parameters.jsCode in the workflow export. Everything outside the
// markers is harness scaffolding.
//
// Usage:
//   const { makeContext } = require('../../n8n-shim');
//   const unit = require('./build-questions');
//   const out = await unit.run(makeContext({ items: [...], nodeOutputs: {...},
//                                            now: '2026-01-01T00:00:00.000Z' }));
'use strict';

// The parameter is deliberately named __n8nCtx: node code frequently declares
// its own `const ctx`, and the wrapper must not shadow or collide with it.
async function run(__n8nCtx) {
  __n8nCtx = __n8nCtx || {};
  const $input = __n8nCtx.$input;
  const $ = __n8nCtx.$;
  const $now = __n8nCtx.$now;
  const $json = __n8nCtx.$json;
  const $items = __n8nCtx.$items;
  void $input; void $; void $now; void $json; void $items;
  // Inner async IIFE: n8n Code nodes may use top-level `await`, and `this`
  // is bound to the context so `this.helpers.*` resolves to the shim's.
  return (async function () {
// ---- BEGIN VERBATIM n8n jsCode ----
function e(s) {
  return String(s || '').replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
}
const p = $('Finalise Plan').first().json;
const qs = (p.questions || []).slice(0, 10);
const lines = [
  'Matter ' + e(p.matter_id),
  'Status: NEEDS_INFORMATION',
  'Playbook: ' + e(p.playbook_name),
  '',
  p.owner_summary ? e(p.owner_summary) + '\n' : '',
  'I need these details before I draft anything:',
  ...qs.map((q, i) => (i + 1) + '. ' + e(q)),
  '',
  'Reply with the answers in one message. Anything you leave out stays recorded as MISSING.',
];
// Without this the owner has no way out of the question loop when a fact simply
// does not exist, which is what kept every matter at NEEDS_INFORMATION forever.
if (p.proceed_hint) lines.push('', e(p.proceed_hint));
let risks = [];
try { risks = JSON.parse(p.risk_flags_json || '[]'); } catch (e2) {}
if (risks.length) lines.push('', 'Flags so far: ' + risks.map(e).join(', '));
return [{ json: { reply: lines.filter(l => l !== undefined).join('\n').slice(0, 3900) } }];
// ---- END VERBATIM n8n jsCode ----
  }).call(__n8nCtx);
}

module.exports = { run, meta: {
  "workflow": "wf2",
  "nodeName": "Build Questions",
  "nodeId": "9b9840de-4764-4ac2-8c7d-c988608d03e8",
  "mode": "runOnceForAllItems",
  "sha256OfJsCode": "d91687ec77f9951265623bfadb239960e93e38989db78f6e562ff9cacbcb2d12"
} };
