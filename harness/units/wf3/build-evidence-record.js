// AUTO-GENERATED by .tooling/extract-units.py -- DO NOT EDIT BY HAND.
// Source workflow : wf3
// Node name       : Build Evidence Record
// Node id         : cf83bea8-e83b-44c4-ab67-cba14a15302f
// Node mode       : runOnceForAllItems
// sha256(jsCode)  : 5fee8c36d3e9211cc89c73e4b361f90b4ed2caaf51409ccdbe765a0eb28716d2
//
// The code between the BEGIN/END markers below is byte-identical to the
// node's parameters.jsCode in the workflow export. Everything outside the
// markers is harness scaffolding.
//
// Usage:
//   const { makeContext } = require('../../n8n-shim');
//   const unit = require('./build-evidence-record');
//   const out = await unit.run(makeContext({ items: [...], nodeOutputs: {...},
//                                            now: '2026-01-01T00:00:00.000Z' }));
'use strict';

// The parameter is deliberately named __n8nCtx: node code frequently declares
// its own `const ctx`, and the wrapper must not shadow or collide with it.
async function run(__n8nCtx) {
  __n8nCtx = __n8nCtx || {};
  const $input = __n8nCtx.$input;
  const $ = __n8nCtx.$;
  const $now = __n8nCtx.$now;
  const $json = __n8nCtx.$json;
  const $items = __n8nCtx.$items;
  void $input; void $; void $now; void $json; void $items;
  // Inner async IIFE: n8n Code nodes may use top-level `await`, and `this`
  // is bound to the context so `this.helpers.*` resolves to the shim's.
  return (async function () {
// ---- BEGIN VERBATIM n8n jsCode ----
const base = $('Hash File').first().json;
const upload = $('Upload to Drive').first().json;
const src = $input.first().json || {};

// Extracted document text is EVIDENCE CONTENT. It is stored, never executed.
// It is written to Google Drive as a text file. It is NOT written to Google Sheets.
let text = String(src.text || src.data || '').trim();
let status = 'COMPLETED';
if (base.evidence_kind === 'OTHER') { status = 'NOT_ATTEMPTED'; text = ''; }
else if (!text) { status = 'FAILED'; }

const truncated = text.length > 200000;
if (truncated) text = text.slice(0, 200000);

const notes = [];
if (base.evidence_kind === 'OTHER') notes.push('Unsupported file type for text extraction. REQUIRES_HUMAN_REVIEW.');
if (status === 'FAILED') notes.push('Text extraction produced nothing. REQUIRES_HUMAN_REVIEW.');
if (truncated) notes.push('Extracted text truncated at 200000 characters.');
if (base.hash === 'NOT_COMPUTED') notes.push('Hash not computed in this runtime.');
if (base.matter_id === 'UNASSIGNED') notes.push('No matter could be resolved. Filed as UNASSIGNED.');

return [{ json: {
  evidence_id: base.evidence_id,
  matter_id: base.matter_id,
  folder_id: base.folder_id,
  file_name: base.safe_file_name,
  file_type: base.mime_type,
  drive_url: String(upload.webViewLink || ('https://drive.google.com/file/d/' + (upload.id || '') + '/view')),
  drive_file_id: String(upload.id || ''),
  source: 'telegram',
  uploaded_at: new Date().toISOString(),
  hash: base.hash,
  size_bytes: base.size_bytes,
  extraction_status: status,
  extracted_chars: text.length,
  reliability: 'USER_PROVIDED',
  notes: notes.join(' '),
  has_text: text.length > 0,
  text_file_name: base.evidence_id + '__extracted.txt',
  // carried in the execution context only. Never written to a Sheets cell.
  extracted_text: text,
  caption: String(base.caption || base.text || ''),
}}];
// ---- END VERBATIM n8n jsCode ----
  }).call(__n8nCtx);
}

module.exports = { run, meta: {
  "workflow": "wf3",
  "nodeName": "Build Evidence Record",
  "nodeId": "cf83bea8-e83b-44c4-ab67-cba14a15302f",
  "mode": "runOnceForAllItems",
  "sha256OfJsCode": "5fee8c36d3e9211cc89c73e4b361f90b4ed2caaf51409ccdbe765a0eb28716d2"
} };
