// AUTO-GENERATED by .tooling/extract-units.py -- DO NOT EDIT BY HAND.
// Source workflow : wf3
// Node name       : Finalise Evidence Row
// Node id         : 54e88e1f-5703-4c60-a18d-de72614eefcd
// Node mode       : runOnceForAllItems
// sha256(jsCode)  : 2f1f98c6ec10ad1f518c56f518a051da8e03a1133b5bf06ea709e7bdaade08d9
//
// The code between the BEGIN/END markers below is byte-identical to the
// node's parameters.jsCode in the workflow export. Everything outside the
// markers is harness scaffolding.
//
// Usage:
//   const { makeContext } = require('../../n8n-shim');
//   const unit = require('./finalise-evidence-row');
//   const out = await unit.run(makeContext({ items: [...], nodeOutputs: {...},
//                                            now: '2026-01-01T00:00:00.000Z' }));
'use strict';

// The parameter is deliberately named __n8nCtx: node code frequently declares
// its own `const ctx`, and the wrapper must not shadow or collide with it.
async function run(__n8nCtx) {
  __n8nCtx = __n8nCtx || {};
  const $input = __n8nCtx.$input;
  const $ = __n8nCtx.$;
  const $now = __n8nCtx.$now;
  const $json = __n8nCtx.$json;
  const $items = __n8nCtx.$items;
  void $input; void $; void $now; void $json; void $items;
  // Inner async IIFE: n8n Code nodes may use top-level `await`, and `this`
  // is bound to the context so `this.helpers.*` resolves to the shim's.
  return (async function () {
// ---- BEGIN VERBATIM n8n jsCode ----
// Merge the text-file result back onto the evidence row.
// extracted_text is deliberately dropped here so it can never reach Google Sheets.
const base = $('Build Evidence Record').first().json;
const t = $input.first().json || {};
const row = { ...base };
delete row.extracted_text;
row.extracted_text_file_id = String(t.id || '');
row.extracted_text_drive_url = String(t.webViewLink || (t.id ? ('https://drive.google.com/file/d/' + t.id + '/view') : ''));
if (base.has_text && !row.extracted_text_file_id) {
  row.notes = (row.notes + ' Text extraction succeeded. I could not store the text file in Drive. REQUIRES_HUMAN_REVIEW.').trim();
  row.extraction_status = 'TEXT_NOT_STORED';
}
return [{ json: row }];
// ---- END VERBATIM n8n jsCode ----
  }).call(__n8nCtx);
}

module.exports = { run, meta: {
  "workflow": "wf3",
  "nodeName": "Finalise Evidence Row",
  "nodeId": "54e88e1f-5703-4c60-a18d-de72614eefcd",
  "mode": "runOnceForAllItems",
  "sha256OfJsCode": "2f1f98c6ec10ad1f518c56f518a051da8e03a1133b5bf06ea709e7bdaade08d9"
} };
