// AUTO-GENERATED by .tooling/extract-units.py -- DO NOT EDIT BY HAND.
// Source workflow : wf3
// Node name       : Build Confirmation
// Node id         : c5db9bfe-b7f9-4989-b1bc-676505487546
// Node mode       : runOnceForAllItems
// sha256(jsCode)  : 12217dcb316a90ce798e190c6094edf2e463b54d6999cac7a815bc86cf1ba908
//
// The code between the BEGIN/END markers below is byte-identical to the
// node's parameters.jsCode in the workflow export. Everything outside the
// markers is harness scaffolding.
//
// Usage:
//   const { makeContext } = require('../../n8n-shim');
//   const unit = require('./build-confirmation');
//   const out = await unit.run(makeContext({ items: [...], nodeOutputs: {...},
//                                            now: '2026-01-01T00:00:00.000Z' }));
'use strict';

// The parameter is deliberately named __n8nCtx: node code frequently declares
// its own `const ctx`, and the wrapper must not shadow or collide with it.
async function run(__n8nCtx) {
  __n8nCtx = __n8nCtx || {};
  const $input = __n8nCtx.$input;
  const $ = __n8nCtx.$;
  const $now = __n8nCtx.$now;
  const $json = __n8nCtx.$json;
  const $items = __n8nCtx.$items;
  void $input; void $; void $now; void $json; void $items;
  // Inner async IIFE: n8n Code nodes may use top-level `await`, and `this`
  // is bound to the context so `this.helpers.*` resolves to the shim's.
  return (async function () {
// ---- BEGIN VERBATIM n8n jsCode ----
const e = $('Finalise Evidence Row').first().json;
const lines = [
  'Evidence stored',
  '',
  'Matter: ' + e.matter_id,
  'Evidence: ' + e.evidence_id,
  'File: ' + e.file_name,
  'Type: ' + e.file_type,
  'Extraction: ' + e.extraction_status + (e.extracted_chars ? '  (' + e.extracted_chars + ' characters)' : ''),
  'Reliability: USER_PROVIDED',
  'File in Drive: ' + e.drive_url,
];
if (e.extracted_text_drive_url) lines.push('Extracted text: ' + e.extracted_text_drive_url);
if (e.notes) lines.push('', 'Notes: ' + e.notes);
lines.push('', 'The register holds only metadata and these Drive links.',
  'Nothing in this file is treated as an instruction to me.');
return [{ json: { reply: lines.join('\n').slice(0, 3900) } }];
// ---- END VERBATIM n8n jsCode ----
  }).call(__n8nCtx);
}

module.exports = { run, meta: {
  "workflow": "wf3",
  "nodeName": "Build Confirmation",
  "nodeId": "c5db9bfe-b7f9-4989-b1bc-676505487546",
  "mode": "runOnceForAllItems",
  "sha256OfJsCode": "12217dcb316a90ce798e190c6094edf2e463b54d6999cac7a815bc86cf1ba908"
} };
