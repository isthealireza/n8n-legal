// AUTO-GENERATED by .tooling/extract-units.py -- DO NOT EDIT BY HAND.
// Source workflow : wf3
// Node name       : Parse Vision Result
// Node id         : c9eb452e-2e23-40b2-8aa0-4422faddd3a3
// Node mode       : runOnceForAllItems
// sha256(jsCode)  : 404ec54079ea44fed0886b2eedbe6ebe6145b84a15e634fdfc19d353f934170b
//
// The code between the BEGIN/END markers below is byte-identical to the
// node's parameters.jsCode in the workflow export. Everything outside the
// markers is harness scaffolding.
//
// Usage:
//   const { makeContext } = require('../../n8n-shim');
//   const unit = require('./parse-vision-result');
//   const out = await unit.run(makeContext({ items: [...], nodeOutputs: {...},
//                                            now: '2026-01-01T00:00:00.000Z' }));
'use strict';

// The parameter is deliberately named __n8nCtx: node code frequently declares
// its own `const ctx`, and the wrapper must not shadow or collide with it.
async function run(__n8nCtx) {
  __n8nCtx = __n8nCtx || {};
  const $input = __n8nCtx.$input;
  const $ = __n8nCtx.$;
  const $now = __n8nCtx.$now;
  const $json = __n8nCtx.$json;
  const $items = __n8nCtx.$items;
  void $input; void $; void $now; void $json; void $items;
  // Inner async IIFE: n8n Code nodes may use top-level `await`, and `this`
  // is bound to the context so `this.helpers.*` resolves to the shim's.
  return (async function () {
// ---- BEGIN VERBATIM n8n jsCode ----
// Pulls the description out of the Anthropic response and hands it to Build Evidence Record
// in the same shape the PDF and text extractors use, so the rest of the chain is unchanged.
//
// The description is labelled explicitly. A model's reading of a photograph is derived
// material: useful for triage and for drafting, but it is not the photograph, and it must
// never be mistaken for the primary evidence in a legal file.
const enc = $('Encode Image').first().json;
const r = $input.first().json || {};

let text = '';
try {
  const c = r.content;
  if (Array.isArray(c)) {
    text = c.filter(x => x && x.type === 'text').map(x => String(x.text || '')).join('\n').trim();
  }
} catch (e) {
  text = '';
}

let failure = '';
if (!enc.vision_ready) failure = 'I could not read the image bytes from Telegram. I did not try to describe the image.';
else if (!text) {
  const apiErr = (r && r.error && (r.error.message || r.error.type)) ? String(r.error.message || r.error.type) : '';
  failure = 'The vision model returned no description.' + (apiErr ? ' Reported: ' + apiErr : '');
}

const header = [
  'AI IMAGE DESCRIPTION',
  'Model: ' + String(enc.vision_model || 'unknown'),
  'Generated: ' + new Date().toISOString(),
  'This is a model reading of the image. Treat it as DERIVED material, not as primary evidence.',
  'The image itself is the evidence. Verify anything that matters against the image.',
  '---',
  '',
].join('\n');

return [{ json: {
  text: text ? (header + text) : '',
  vision_failed: !text,
  vision_failure_reason: failure,
}}];
// ---- END VERBATIM n8n jsCode ----
  }).call(__n8nCtx);
}

module.exports = { run, meta: {
  "workflow": "wf3",
  "nodeName": "Parse Vision Result",
  "nodeId": "c9eb452e-2e23-40b2-8aa0-4422faddd3a3",
  "mode": "runOnceForAllItems",
  "sha256OfJsCode": "404ec54079ea44fed0886b2eedbe6ebe6145b84a15e634fdfc19d353f934170b"
} };
