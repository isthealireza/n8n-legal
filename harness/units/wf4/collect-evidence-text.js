// AUTO-GENERATED by .tooling/extract-units.py -- DO NOT EDIT BY HAND.
// Source workflow : wf4
// Node name       : Collect Evidence Text
// Node id         : 139d7cf5-36e2-4660-a037-05de91e80ab2
// Node mode       : runOnceForAllItems
// sha256(jsCode)  : fc569488e5c06cadcedc3aafa3c22e88f6111d09ad9a7f66bc21f5147975de7c
//
// The code between the BEGIN/END markers below is byte-identical to the
// node's parameters.jsCode in the workflow export. Everything outside the
// markers is harness scaffolding.
//
// Usage:
//   const { makeContext } = require('../../n8n-shim');
//   const unit = require('./collect-evidence-text');
//   const out = await unit.run(makeContext({ items: [...], nodeOutputs: {...},
//                                            now: '2026-01-01T00:00:00.000Z' }));
'use strict';

// The parameter is deliberately named __n8nCtx: node code frequently declares
// its own `const ctx`, and the wrapper must not shadow or collide with it.
async function run(__n8nCtx) {
  __n8nCtx = __n8nCtx || {};
  const $input = __n8nCtx.$input;
  const $ = __n8nCtx.$;
  const $now = __n8nCtx.$now;
  const $json = __n8nCtx.$json;
  const $items = __n8nCtx.$items;
  void $input; void $; void $now; void $json; void $items;
  // Inner async IIFE: n8n Code nodes may use top-level `await`, and `this`
  // is bound to the context so `this.helpers.*` resolves to the shim's.
  return (async function () {
// ---- BEGIN VERBATIM n8n jsCode ----
// Reads the SELECTOR's output, not Build Draft Context. Reselect Action If Drafted
// corrects the chosen action, the recipient and the plan; rebuilding the context from
// Build Draft Context here silently reverted all of it, and the owner was asked to
// approve a letter for an action from a superseded plan (exec 265).
const ctx = $('Reselect Action If Drafted').first().json;
const files = ctx.evidence_text_files || [];
const items = $input.all();
const parts = [];
items.forEach((it, i) => {
  const meta = files[i] || {};
  const t = String((it.json && (it.json.text || it.json.data)) || '').trim();
  if (!meta.evidence_id) return;
  parts.push({
    evidence_id: meta.evidence_id,
    file_name: meta.file_name,
    reliability: meta.reliability,
    text: t ? t.slice(0, 6000) : '(the stored text could not be read back from Drive)',
  });
});
return [{ json: { ...ctx, evidence_texts: parts, evidence_texts_loaded: parts.length } }];
// ---- END VERBATIM n8n jsCode ----
  }).call(__n8nCtx);
}

module.exports = { run, meta: {
  "workflow": "wf4",
  "nodeName": "Collect Evidence Text",
  "nodeId": "139d7cf5-36e2-4660-a037-05de91e80ab2",
  "mode": "runOnceForAllItems",
  "sha256OfJsCode": "fc569488e5c06cadcedc3aafa3c22e88f6111d09ad9a7f66bc21f5147975de7c"
} };
