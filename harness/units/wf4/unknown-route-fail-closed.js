// AUTO-GENERATED by .tooling/extract-units.py -- DO NOT EDIT BY HAND.
// Source workflow : wf4
// Node name       : Unknown Route - Fail Closed
// Node id         : c2bc10bf-f046-4faa-997b-8e22453678ed
// Node mode       : runOnceForAllItems
// sha256(jsCode)  : 90cc41bb52238e6eb2c48e88706018d39dbd291c2883ebcd48e968c2383a9bba
//
// The code between the BEGIN/END markers below is byte-identical to the
// node's parameters.jsCode in the workflow export. Everything outside the
// markers is harness scaffolding.
//
// Usage:
//   const { makeContext } = require('../../n8n-shim');
//   const unit = require('./unknown-route-fail-closed');
//   const out = await unit.run(makeContext({ items: [...], nodeOutputs: {...},
//                                            now: '2026-01-01T00:00:00.000Z' }));
'use strict';

// The parameter is deliberately named __n8nCtx: node code frequently declares
// its own `const ctx`, and the wrapper must not shadow or collide with it.
async function run(__n8nCtx) {
  __n8nCtx = __n8nCtx || {};
  const $input = __n8nCtx.$input;
  const $ = __n8nCtx.$;
  const $now = __n8nCtx.$now;
  const $json = __n8nCtx.$json;
  const $items = __n8nCtx.$items;
  void $input; void $; void $now; void $json; void $items;
  // Inner async IIFE: n8n Code nodes may use top-level `await`, and `this`
  // is bound to the context so `this.helpers.*` resolves to the shim's.
  return (async function () {
// ---- BEGIN VERBATIM n8n jsCode ----
// Unknown Route - Fail Closed
// ===========================================================================
// The Mode switch had fallbackOutput wired to Build Draft Context, so a
// route_group the workflow does not implement was silently treated as DRAFT.
// That is fail-open on an unknown route, which safety rule 6 forbids: an
// unrecognised instruction must stop, not pick the nearest branch.
//
// This node performs no I/O. It converts the unknown route into the same halt
// shape the Integrity Guard emits, so it reaches the existing containment
// terminus and the owner notification built on it.
// ===========================================================================

const cfg = (() => { try { return $('Config').first().json || {}; } catch (e) { return {}; } })();
const passthrough = (() => { try { return $input.first().json || {}; } catch (e) { return {}; } })();

const rg = String(cfg.route_group === null || cfg.route_group === undefined ? '' : cfg.route_group).trim();

return [{ json: Object.assign({}, passthrough, {
  integrity_ok: false,
  gate: 'DATA_INTEGRITY_CONFLICT',
  integrity_code: 'UNKNOWN_ROUTE_GROUP',
  integrity_reason: 'route_group ' + (rg ? '"' + rg + '"' : '(empty)')
    + ' is not a route this workflow implements. The routes are DRAFT and APPROVAL. '
    + 'I stopped instead of assuming DRAFT.',
  integrity_detail: [],
}) }];
// ---- END VERBATIM n8n jsCode ----
  }).call(__n8nCtx);
}

module.exports = { run, meta: {
  "workflow": "wf4",
  "nodeName": "Unknown Route - Fail Closed",
  "nodeId": "c2bc10bf-f046-4faa-997b-8e22453678ed",
  "mode": "runOnceForAllItems",
  "sha256OfJsCode": "90cc41bb52238e6eb2c48e88706018d39dbd291c2883ebcd48e968c2383a9bba"
} };
