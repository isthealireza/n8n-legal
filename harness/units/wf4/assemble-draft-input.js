// AUTO-GENERATED by .tooling/extract-units.py -- DO NOT EDIT BY HAND.
// Source workflow : wf4
// Node name       : Assemble Draft Input
// Node id         : 8ca45acc-7e68-462e-9b88-15c83bcbb993
// Node mode       : runOnceForAllItems
// sha256(jsCode)  : a4661f30feea6fca8a5a37c62f9f910d6b2636c53e5ce287192ccf78a0aee1dc
//
// The code between the BEGIN/END markers below is byte-identical to the
// node's parameters.jsCode in the workflow export. Everything outside the
// markers is harness scaffolding.
//
// Usage:
//   const { makeContext } = require('../../n8n-shim');
//   const unit = require('./assemble-draft-input');
//   const out = await unit.run(makeContext({ items: [...], nodeOutputs: {...},
//                                            now: '2026-01-01T00:00:00.000Z' }));
'use strict';

// The parameter is deliberately named __n8nCtx: node code frequently declares
// its own `const ctx`, and the wrapper must not shadow or collide with it.
async function run(__n8nCtx) {
  __n8nCtx = __n8nCtx || {};
  const $input = __n8nCtx.$input;
  const $ = __n8nCtx.$;
  const $now = __n8nCtx.$now;
  const $json = __n8nCtx.$json;
  const $items = __n8nCtx.$items;
  void $input; void $; void $now; void $json; void $items;
  // Inner async IIFE: n8n Code nodes may use top-level `await`, and `this`
  // is bound to the context so `this.helpers.*` resolves to the shim's.
  return (async function () {
// ---- BEGIN VERBATIM n8n jsCode ----
const c = $('Distil Sources').first().json;
const ev = (c.evidence_texts || []).map(e =>
  '<evidence id="' + e.evidence_id + '" file="' + e.file_name + '" reliability="' + e.reliability + '">\n'
  + e.text + '\n</evidence>').join('\n\n');
const idx = (c.evidence_index || []).map(e =>
  '- ' + e.evidence_id + ' ' + e.file_name + ' (' + e.file_type + ', ' + e.extraction_status + ')').join('\n');
const src = (c.sources || []).filter(s => s.verification_status === 'RETRIEVED').map(s =>
  '<source id="' + s.source_id + '" url="' + s.url + '" publisher="' + s.publisher
  + '" retrieved_at="' + s.retrieved_at + '">\n' + (s.excerpt || '') + '\n</source>').join('\n\n');
const failed = (c.sources || []).filter(s => s.verification_status !== 'RETRIEVED').map(s => s.url);
return [{ json: { ...c,
  evidence_block: ev || '(this matter has no readable evidence text on file)',
  evidence_index_block: idx || '(no evidence filed)',
  sources_block: src || '(no source could be retrieved)',
  failed_sources: failed.join(', ') || 'none',
  checklist_block: (c.issue_checklist || []).map((x, i) => (i + 1) + '. ' + x).join('\n') } }];
// ---- END VERBATIM n8n jsCode ----
  }).call(__n8nCtx);
}

module.exports = { run, meta: {
  "workflow": "wf4",
  "nodeName": "Assemble Draft Input",
  "nodeId": "8ca45acc-7e68-462e-9b88-15c83bcbb993",
  "mode": "runOnceForAllItems",
  "sha256OfJsCode": "a4661f30feea6fca8a5a37c62f9f910d6b2636c53e5ce287192ccf78a0aee1dc"
} };
