// AUTO-GENERATED by .tooling/extract-units.py -- DO NOT EDIT BY HAND.
// Source workflow : wf4
// Node name       : Send Conflict Notice (PLACEHOLDER - NO SENDER BUILT)
// Node id         : 05b039ad-bbc5-4732-ba86-5616b9323c98
// Node mode       : runOnceForAllItems
// sha256(jsCode)  : b207bcf1013061c333b2b38c445801036491546054289c59d45686207fa49dd7
//
// The code between the BEGIN/END markers below is byte-identical to the
// node's parameters.jsCode in the workflow export. Everything outside the
// markers is harness scaffolding.
//
// Usage:
//   const { makeContext } = require('../../n8n-shim');
//   const unit = require('./send-conflict-notice-placeholder-no-sender-built');
//   const out = await unit.run(makeContext({ items: [...], nodeOutputs: {...},
//                                            now: '2026-01-01T00:00:00.000Z' }));
'use strict';

// The parameter is deliberately named __n8nCtx: node code frequently declares
// its own `const ctx`, and the wrapper must not shadow or collide with it.
async function run(__n8nCtx) {
  __n8nCtx = __n8nCtx || {};
  const $input = __n8nCtx.$input;
  const $ = __n8nCtx.$;
  const $now = __n8nCtx.$now;
  const $json = __n8nCtx.$json;
  const $items = __n8nCtx.$items;
  void $input; void $; void $now; void $json; void $items;
  // Inner async IIFE: n8n Code nodes may use top-level `await`, and `this`
  // is bound to the context so `this.helpers.*` resolves to the shim's.
  return (async function () {
// ---- BEGIN VERBATIM n8n jsCode ----
// PLACEHOLDER. No sender is built. This node performs no I/O of any kind:
// no HTTP, no Telegram, no Gmail. It exists so the branch is complete and
// testable, and so that publishing WF4 cannot deliver anything by accident.
//
// NOT_IMPLEMENTED is handled explicitly downstream: no status change, no
// attempt consumed, notified_at untouched. The notice stays PENDING and keeps
// appearing in the daily digest, which is the honest state until a real sender
// is approved and built.
const n = $input.first().json;
return [{ json: { ...n, send_outcome: 'NOT_IMPLEMENTED',
  send_error: '', sender_note: 'No notification sender is built in this draft.' } }];
// ---- END VERBATIM n8n jsCode ----
  }).call(__n8nCtx);
}

module.exports = { run, meta: {
  "workflow": "wf4",
  "nodeName": "Send Conflict Notice (PLACEHOLDER - NO SENDER BUILT)",
  "nodeId": "05b039ad-bbc5-4732-ba86-5616b9323c98",
  "mode": "runOnceForAllItems",
  "sha256OfJsCode": "b207bcf1013061c333b2b38c445801036491546054289c59d45686207fa49dd7"
} };
