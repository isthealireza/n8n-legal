// AUTO-GENERATED by .tooling/extract-units.py -- DO NOT EDIT BY HAND.
// Source workflow : wf4
// Node name       : Build Dry Run Report
// Node id         : 05ed9ab8-cb7b-4cdd-a224-ff84a08387d2
// Node mode       : runOnceForAllItems
// sha256(jsCode)  : 7c5ad5410a2c63e66670e573d54cf150ca1fcc0a0f20455504f7f5384c20bac0
//
// The code between the BEGIN/END markers below is byte-identical to the
// node's parameters.jsCode in the workflow export. Everything outside the
// markers is harness scaffolding.
//
// Usage:
//   const { makeContext } = require('../../n8n-shim');
//   const unit = require('./build-dry-run-report');
//   const out = await unit.run(makeContext({ items: [...], nodeOutputs: {...},
//                                            now: '2026-01-01T00:00:00.000Z' }));
'use strict';

// The parameter is deliberately named __n8nCtx: node code frequently declares
// its own `const ctx`, and the wrapper must not shadow or collide with it.
async function run(__n8nCtx) {
  __n8nCtx = __n8nCtx || {};
  const $input = __n8nCtx.$input;
  const $ = __n8nCtx.$;
  const $now = __n8nCtx.$now;
  const $json = __n8nCtx.$json;
  const $items = __n8nCtx.$items;
  void $input; void $; void $now; void $json; void $items;
  // Inner async IIFE: n8n Code nodes may use top-level `await`, and `this`
  // is bound to the context so `this.helpers.*` resolves to the shim's.
  return (async function () {
// ---- BEGIN VERBATIM n8n jsCode ----
const g = $('Approval Gate').first().json;
const body = g.delivery_mode === 'EMAIL_BODY'
  ? g.content
  : (g.cover_note || 'Please find the document attached.');
const artefact = {
  EMAIL_BODY: 'no file. The draft text would be the email body.',
  PDF_ATTACHMENT: 'a Google Doc "' + g.document_name + '" in the matter folder, exported to PDF and attached.',
  DOCX_ATTACHMENT: 'a Google Doc "' + g.document_name + '" in the matter folder, exported to DOCX and attached.',
  DRIVE_LINK: 'a Google Doc "' + g.document_name + '" in the matter folder, shared with '
    + g.recipient + ' as reader, and the link put in the email body.',
}[g.delivery_mode];

const lines = [
  'DRY RUN. Nothing was sent and nothing was shared.',
  '',
  'Every approval check passed. This is what would have happened:',
  '',
  'Matter: ' + g.matter_id + '  ' + g.matter_title,
  'Action: ' + g.action_id,
  'Draft: ' + g.draft_id + '  (hash ' + g.content_hash + ')',
  'Approval: ' + g.approval_id,
  'Delivery: ' + g.delivery_mode,
  'Would create: ' + artefact,
  'Gmail To: ' + g.recipient,
  'Gmail Subject: ' + g.subject,
  '',
  '--- EMAIL BODY THAT WOULD BE SENT ---',
  body.slice(0, 1800),
  '--- END ---',
  '',
  'State was not changed. The approval is still PENDING and the action is still AWAITING_APPROVAL.',
  'Set dry_run to false in the Config node of workflow 4, then approve again to send for real.',
];
return [{ json: { reply: lines.join('\n').slice(0, 3900),
  intended: JSON.stringify({ matter_id: g.matter_id, action_id: g.action_id, draft_id: g.draft_id,
    approval_id: g.approval_id, delivery_mode: g.delivery_mode, recipient: g.recipient,
    subject: g.subject, content_hash: g.content_hash }) } }];
// ---- END VERBATIM n8n jsCode ----
  }).call(__n8nCtx);
}

module.exports = { run, meta: {
  "workflow": "wf4",
  "nodeName": "Build Dry Run Report",
  "nodeId": "05ed9ab8-cb7b-4cdd-a224-ff84a08387d2",
  "mode": "runOnceForAllItems",
  "sha256OfJsCode": "7c5ad5410a2c63e66670e573d54cf150ca1fcc0a0f20455504f7f5384c20bac0"
} };
