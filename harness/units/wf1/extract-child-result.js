// AUTO-GENERATED by .tooling/extract-units.py -- DO NOT EDIT BY HAND.
// Source workflow : wf1
// Node name       : Extract Child Result
// Node id         : 10810d44-0d75-4f31-95a7-57fd566db654
// Node mode       : runOnceForAllItems
// sha256(jsCode)  : 28ea1b094199471f4b2da9f0064fc3f0ea149220266da7d9add781c6a511d236
//
// The code between the BEGIN/END markers below is byte-identical to the
// node's parameters.jsCode in the workflow export. Everything outside the
// markers is harness scaffolding.
//
// Usage:
//   const { makeContext } = require('../../n8n-shim');
//   const unit = require('./extract-child-result');
//   const out = await unit.run(makeContext({ items: [...], nodeOutputs: {...},
//                                            now: '2026-01-01T00:00:00.000Z' }));
'use strict';

// The parameter is deliberately named __n8nCtx: node code frequently declares
// its own `const ctx`, and the wrapper must not shadow or collide with it.
async function run(__n8nCtx) {
  __n8nCtx = __n8nCtx || {};
  const $input = __n8nCtx.$input;
  const $ = __n8nCtx.$;
  const $now = __n8nCtx.$now;
  const $json = __n8nCtx.$json;
  const $items = __n8nCtx.$items;
  void $input; void $; void $now; void $json; void $items;
  // Inner async IIFE: n8n Code nodes may use top-level `await`, and `this`
  // is bound to the context so `this.helpers.*` resolves to the shim's.
  return (async function () {
// ---- BEGIN VERBATIM n8n jsCode ----
// Structured return from WF2 Set Return Data node.
// Primary session write already happened inside WF2 (Persist Session node).
// This is the belt-and-suspenders write in case WF2's Persist Session failed.
//
// Fail-closed rules:
//   Never overwrite a valid existing active_matter_id with empty.
//   Do not write if matter_id or chat_id is missing.
//   Do not change another user's session (chat_id guard).
const fromChild = $input.first().json || {};
const route = $('Resolve Route').first().json;

const childMatterId    = String(fromChild.matter_id    || '').trim().toUpperCase();
const childStatus      = String(fromChild.matter_status || '').trim();
const childAwaiting    = String(fromChild.awaiting      || '').trim();
const childAwaitingRef = String(fromChild.awaiting_ref  || '').trim();
const childChatId      = String(fromChild.chat_id       || '').trim();
const ownChatId        = String(route.chat_id           || '').trim();

// Guard: child must belong to this session's chat
const chatMatch = !!childChatId && childChatId === ownChatId;

// Guard: child must have returned a valid matter_id and session_ok
const childSessionOk = fromChild.session_ok === true
                       && !!childMatterId
                       && chatMatch;

// Guard: never overwrite a valid existing matter_id with empty
const existingMatterId = String(route.session_matter_id || '').trim().toUpperCase();
const activeMatterId = childSessionOk
  ? childMatterId
  : (existingMatterId || '');

const awaiting = childSessionOk
  ? childAwaiting
  : String(route.session_awaiting || '').trim();

const awaitingRef = childSessionOk
  ? childAwaitingRef
  : String(route.session_awaiting_ref || '').trim();

return [{ json: {
  chat_id:           ownChatId,
  active_matter_id:  activeMatterId,
  awaiting:          awaiting,
  awaiting_ref:      awaitingRef,
  last_route:        String(route.route || 'NEW_MATTER'),
  _child_session_ok: childSessionOk,
}}];
// ---- END VERBATIM n8n jsCode ----
  }).call(__n8nCtx);
}

module.exports = { run, meta: {
  "workflow": "wf1",
  "nodeName": "Extract Child Result",
  "nodeId": "10810d44-0d75-4f31-95a7-57fd566db654",
  "mode": "runOnceForAllItems",
  "sha256OfJsCode": "28ea1b094199471f4b2da9f0064fc3f0ea149220266da7d9add781c6a511d236"
} };
