// AUTO-GENERATED by .tooling/extract-units.py -- DO NOT EDIT BY HAND.
// Source workflow : wf5
// Node name       : Build Reply Notice
// Node id         : 7d73d2a8-6e37-479b-8dfc-730df61288b4
// Node mode       : runOnceForAllItems
// sha256(jsCode)  : d93e2a6e19d85ce9dbd0ba646b4caad2d0996e69f746fa78a7ad2f1098355f59
//
// The code between the BEGIN/END markers below is byte-identical to the
// node's parameters.jsCode in the workflow export. Everything outside the
// markers is harness scaffolding.
//
// Usage:
//   const { makeContext } = require('../../n8n-shim');
//   const unit = require('./build-reply-notice');
//   const out = await unit.run(makeContext({ items: [...], nodeOutputs: {...},
//                                            now: '2026-01-01T00:00:00.000Z' }));
'use strict';

// The parameter is deliberately named __n8nCtx: node code frequently declares
// its own `const ctx`, and the wrapper must not shadow or collide with it.
async function run(__n8nCtx) {
  __n8nCtx = __n8nCtx || {};
  const $input = __n8nCtx.$input;
  const $ = __n8nCtx.$;
  const $now = __n8nCtx.$now;
  const $json = __n8nCtx.$json;
  const $items = __n8nCtx.$items;
  void $input; void $; void $now; void $json; void $items;
  // Inner async IIFE: n8n Code nodes may use top-level `await`, and `this`
  // is bound to the context so `this.helpers.*` resolves to the shim's.
  return (async function () {
// ---- BEGIN VERBATIM n8n jsCode ----
const r = $('Validate Reply JSON').first().json;
const lines = [
  'Reply received',
  '',
  'Matter: ' + r.matter_id + '  ' + r.matter_title,
  'Action: ' + (r.action_id || 'not identified'),
  'From: ' + r.from_address,
  'Subject: ' + r.subject,
  'Classification: ' + r.classification,
  'Logged as: ' + r.communication_id,
  '',
  'Summary: ' + r.summary,
];
if (r.requests.length) lines.push('', 'They asked for:', ...r.requests.map(x => '- ' + x));
if (r.deadline_date) lines.push('', 'Date mentioned: ' + r.deadline_date + '  (' + r.deadline_basis + ')');
if (r.needs_owner_decision) {
  lines.push('', 'This is an offer or a proposal.',
    'I have not accepted or rejected anything, and I will not.',
    'Action ' + r.next_action_id + ' needs your decision.');
} else if (r.create_action) {
  lines.push('', 'Next action created: ' + r.next_action_id + '  ' + r.next_action_type);
}
if (!r.json_valid) lines.push('', 'The classifier failed on this email. Read it in Gmail before acting.');
lines.push('', 'Reply here to tell me what to do next.');
return [{ json: { reply: lines.join('\n').slice(0, 3900) } }];
// ---- END VERBATIM n8n jsCode ----
  }).call(__n8nCtx);
}

module.exports = { run, meta: {
  "workflow": "wf5",
  "nodeName": "Build Reply Notice",
  "nodeId": "7d73d2a8-6e37-479b-8dfc-730df61288b4",
  "mode": "runOnceForAllItems",
  "sha256OfJsCode": "d93e2a6e19d85ce9dbd0ba646b4caad2d0996e69f746fa78a7ad2f1098355f59"
} };
