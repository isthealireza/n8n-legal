# Drift report

**Generated by `scripts/capture_mcp.py` — NOT by `scripts/sync.py`.**

Every body summarised below was read through the authenticated,
read-only **n8n MCP server** on **2026-08-25**, because the n8n public REST
API was unreachable from the capture environment (the egress proxy
refused `CONNECT` to the instance host and no API key existed). The
REST path in `scripts/sync.py` has still never run against a live
instance. See `docs/API_CAPABILITIES.md`.

A workflow's draft/published state is live and this is a snapshot of
one day. Read the date, not the tense.

## Published vs draft, as observed

| key | name | published (active) version id | draft version id | differ? | draft file |
|---|---|---|---|---|---|
| WF1 | 1 - Telegram Intake and Command Router | `b1c425bf-5830-4e3c-a6f7-bcf2c02ef593` | `9cc644ec-595d-4084-b64d-cfbe265b0712` | **yes** | `exports/draft/WF1.json` |
| WF2 | 2 - Matter Classification and Planning | `61e6ffee-6708-453e-ab64-8a93dfcbaa9d` | `61e6ffee-6708-453e-ab64-8a93dfcbaa9d` | no | none (draft == published) |
| WF3 | 3 - Evidence Intake and Storage | `644bbdb3-6e78-42c1-a250-3cf9ba05fadc` | `644bbdb3-6e78-42c1-a250-3cf9ba05fadc` | no | none (draft == published) |
| WF4 | 4 - Research Drafting Approval and Dispatch | `8f6b3704-7a7d-47e2-be9f-d40e7311855e` | `8f6b3704-7a7d-47e2-be9f-d40e7311855e` | no | none (draft == published) |
| WF5 | 5 - Inbound Replies and Daily Supervisor | `983da561-5c19-42db-a2b4-1e2e7ac67e0f` | `811b746c-869c-4fc3-94dc-123f60cb7067` | **yes** | `exports/draft/WF5.json` |
| WF9 | 9 - Error Handler | `a5b10619-31e4-4246-8988-90a08bd45ab1` | `a5b10619-31e4-4246-8988-90a08bd45ab1` | no | none (draft == published) |

## How the diverged workflows differ

### WF1 — 1 - Telegram Intake and Command Router

Published `b1c425bf-5830-4e3c-a6f7-bcf2c02ef593` -> draft `9cc644ec-595d-4084-b64d-cfbe265b0712`, per `get_workflow_versions_diff`:

- node `DeepSeek - Router`, `parameters.options.maxTokens`: 64000  ->  32768
- node `Ack Button`, `parameters.operation`: key removed (was answerQuery)

Independent comparison of the two bodies in `exports/`, node by
node — this is computed here, not taken from the diff endpoint,
and it can therefore show differences that endpoint does not
report:

- node `Ack Button`: differs on `parameters`, `position`
- node `DeepSeek - Router`: differs on `parameters`
- node `Is Callback?`: differs on `position`
- the connection map is identical — no node was rewired

### WF5 — 5 - Inbound Replies and Daily Supervisor

Published `983da561-5c19-42db-a2b4-1e2e7ac67e0f` -> draft `811b746c-869c-4fc3-94dc-123f60cb7067`, per `get_workflow_versions_diff`:

- node `Build Daily Digest`, `parameters.jsCode`: long text rewritten: 23691 -> 20956 characters, 433 -> 412 lines (104 line(s) removed, 83 added). The two bodies are in `exports/active/` and `exports/draft/`.

Independent comparison of the two bodies in `exports/`, node by
node — this is computed here, not taken from the diff endpoint,
and it can therefore show differences that endpoint does not
report:

- node `Build Daily Digest`: differs on `parameters`
- the connection map is identical — no node was rewired

## Changed since the previous manifest

- WF1: first capture
- WF2: first capture
- WF3: first capture
- WF4: first capture
- WF5: first capture
- WF9: first capture

## Digests

SHA-256 over the canonical form (`sync.canonical_json`) of the
**scrubbed** body — the same function `sync.py` hashes with, so a
later REST sync of an unchanged workflow produces the same digest
for the same bytes.

| key | source | active sha256 (first 12) | draft sha256 (first 12) |
|---|---|---|---|
| WF1 | mcp-session | `d92d2aceee6f` | `0b0a333007dc` |
| WF2 | mcp-session | `33017c41261b` | — |
| WF3 | mcp-session | `f4184707ec2f` | — |
| WF4 | mcp-session | `ae1c31a4a715` | — |
| WF5 | mcp-session | `661cedbc4c8d` | `257249c38cfa` |
| WF9 | mcp-session | `5cad47fd66f6` | — |
